# PAGE_STATUS.md — Nia Nutrition

Status values: **Not Started** · **In Progress** · **Review** · **Completed**

Update this file the moment a page's status changes — do not batch updates at phase end. This is the single place anyone on the project can check "is X page done yet."

**Language rule (client-confirmed 2026-07-08):** every page below must have both an English (default) and a Kiswahili version completed via WPML before it is marked **Completed** — a page with only its English version done should stay at **In Progress**/**Review**, not Completed. **Currently on hold:** WPML is not yet installed (client decision 2026-07-07, see `RISKS.md` R20) — no page can reach Completed until it's unblocked.

**Phase 2 (Theme Foundation) — done 2026-07-07:** the Nia Theme is scaffolded and active (`wp-content/themes/nia-theme`) — `style.css`, `functions.php`, `theme.json`, base templates (`index.php`, `page.php`, `single.php`, `archive.php`, `404.php`), minimal `header.php`/`footer.php` stubs (real markup is Phase 3). Full token set compiled via Tailwind (`tailwind.config.js`) and mirrored in `theme.json`'s block-editor palette/font sizes. Fonts self-hosted (Playfair Display, Montserrat, Material Symbols Outlined — latin/latin-ext subsets only, no CDN requests). Elementor's Global Colors/Fonts configured to mirror the same tokens (curated subset — see `ARCHITECTURE.md` §2a). Verified: blank pages render correctly (home, single post, static page, WooCommerce shop archive, 404), no PHP errors/warnings, WPCS clean.

**Phase 4 (Static Marketing Pages) — English builds done 2026-07-07.** All 10 pages below are live, responsive, and match their mockups' design language using dedicated `page-*.php` templates (per-page bespoke layouts) that reuse the Phase 3 block library (`nia/hero`, `nia/benefit-grid`, `nia/testimonial`, `nia/cta-banner`, `nia/newsletter-signup`) via `render_block()` for repeating sections. **Every page is capped at Review, not Completed** — the language rule above requires a Kiswahili version via WPML, which is on hold (R20). Two further known gaps, both already tracked in `RISKS.md`: **(1)** all photography is the mockups' original AI-generated placeholder imagery, now self-hosted in `wp-content/themes/nia-theme/assets/images/placeholders/` (fixed page images) or the media library (blog post featured images) instead of hotlinked from Google — still needs replacing with real product/lifestyle photography before launch (R6). **(2)** the 4 policy pages contain original draft copy (no client-provided legal text existed to transcribe) and are visibly marked "pending final legal review" on-page — see R22 below. A real `wp_nav_menu` ("Primary Navigation") now backs the header, replacing the Phase 3 fallback; `Nia_Nav_Walker`'s active-state highlighting verified working.

---

## Static Marketing Pages (Phase 4)

| Page | Source mockup | Status | Notes |
|---|---|---|---|
| Homepage | `index.html` | Review | `page-homepage.php`, set as the static front page. Featured Products section is a static 4-card placeholder — Phase 5 wires a live WooCommerce query (`ARCHITECTURE.md` §9a) |
| Our Heritage / About | `about-nia.html` | Review | `page-about.php`, slug `/about/`. Does NOT use `about.html` — abandoned concept, see `DESIGN_SYSTEM.md` §0 |
| Contact | `contact.html` | Review | `page-contact.php`, slug `/contact/`. Form is a real Fluent Forms form (#5, "Contact Page Inquiry") with matching fields, not dead markup |
| FAQ | `faqs.html` | Review | `page-faq.php`, slug `/faq/`. Re-themed from stale olive-gold to canonical orange-gold; accordion + category tabs rebuilt with Alpine.js. Added a "Payments" FAQ block (the mockup's category grid links to one but no content block existed in source) |
| Wellness Journal — index | `journal.html` | Review | `page-journal.php`, slug `/journal/`. Featured article = most recent post; grid = next 3; category bar links to real WP categories (Recipes/Science/Lifestyle/Community/Tradition) styled via `archive.php` |
| Wellness Journal — single post template | `journal.html` (article layout) | Review | `single.php`. No dedicated single-post mockup exists in source (journal.html is index-only) — layout is a reasonable extrapolation using the same tokens (hero image, eyebrow, `.entry-content` prose styling, related articles) |
| The Ritual / Subscription info page | `ritual.html` / `subscription.html` | Review | `page-subscription.php`, slug `/subscription/`. Built from `subscription.html` (chosen over `ritual.html` — near-identical; "Subscription" is the nav label used sitewide, see `DESIGN_SYSTEM.md` §0) |
| Shipping Policy | none (new content) | Review | `/shipping-policy/`, using base `page.php`. Original draft copy, marked "pending final legal review" on-page |
| Refund Policy | none (new content) | Review | `/refund-policy/`, using base `page.php`. Original draft copy, marked "pending final legal review" on-page |
| Privacy Policy | none (new content) | Review | `/privacy-policy/`, using base `page.php`. Original draft copy, marked "pending final legal review" on-page |
| Terms of Service | none (new content) | Review | `/terms-of-service/`, using base `page.php`. Original draft copy, marked "pending final legal review" on-page |

## WooCommerce Pages (Phases 5–6)

**Phase 5 (WooCommerce Integration) — core plumbing done 2026-07-08.** Catalog is **dummy/placeholder data** — see the note at the end of this section for exactly where the admin edits it going forward.

**Phase 6 (Custom WooCommerce Design) — done 2026-07-08.** Every WooCommerce-rendered page now visually matches its mockup (re-themed to the canonical orange-gold palette where the source used the stale olive-gold set — Checkout). All template overrides live in `wp-content/themes/nia-theme/woocommerce/` (WooCommerce's own sanctioned override mechanism); Cart/Checkout stayed block-based per `ARCHITECTURE.md` §6 and were re-themed via CSS targeting WooCommerce Blocks' own stable class names plus direct edits to each page's block content (cross-sells heading, trust-row/microcopy content, native `showFormStepNumbers` step framing) rather than reimplementing the blocks.

**Real-browser verification (2026-07-08, later same day):** Shop archive + Product Detail Page were checked with an actual headless browser (Playwright/Chromium, installed to `/private/tmp/.../scratchpad/browser-check/` — not part of the repo, a throwaway verification tool) at mobile (390px) and desktop (1440px), after the client reported visibly broken layout from screenshots. This caught three real bugs curl/Store-API testing had missed, all now fixed:
1. A default WordPress "Shop sidebar" widget area (Search/Pages/Archives/Categories, from a fresh install's still-assigned default widgets) was rendering after the product grid and after Related Products — both WooCommerce's Shop archive and single-product templates call `do_action('woocommerce_sidebar')`, which this theme never uses. Fixed by unhooking `woocommerce_get_sidebar` (`Nia_Woocommerce::remove_woocommerce_sidebar()`).
2. WooCommerce's own default frontend stylesheets (`woocommerce.css`, `woocommerce-layout.css`, and especially the mobile-only `woocommerce-smallscreen.css`) were still loading alongside this theme's Tailwind stylesheet and fighting it with higher-specificity rules — product card badges/quick-add overlays lost their absolute positioning and collapsed into normal document flow, and card titles picked up a stray list-style bullet. Fixed via WooCommerce's own `woocommerce_enqueue_styles` filter (`functions.php`), which also then exposed that WC's default stylesheet had been the *only* source of the product grid's column layout — added `ul.products { grid ... }` to `input.css` to replace it (scoped to `ul.products` specifically, not a bare `.products` class, since WooCommerce's related-products wrapper section also carries a literal `products` class token that a bare selector would have wrongly matched too).
3. `woocommerce/single-product/related.php`'s item wrapper was a `<div>`, not a `<ul>` — `content-product.php`'s root `<li>` needs a real list ancestor to inherit `list-style: none` from, otherwise the browser's default stylesheet gives it its bullet back. Changed to a real `<ul class="products">`.

**Cart/Checkout real-browser verification (2026-07-21):** Playwright screenshots of Cart and Checkout (mobile + desktop, real populated line items via a real add-to-cart) found and fixed four selectors in `input.css` that didn't match WooCommerce Blocks' actual rendered markup — `.wc-block-cart-item` should be `.wc-block-cart-items__row`, `.wc-block-cart-item__product-name` should be `.wc-block-components-product-name`, `.wc-proceed-to-checkout .wc-block-cart__submit-button` never matched anything (no such wrapper exists — corrected to `.wc-block-cart__submit-button` directly), and the radio-control checked-state modifier is `__option-checked`, not `__option--checked-option`. Confirmed against the live DOM before and after the fix. All four pages — Shop, PDP, Cart, Checkout — are now real-browser verified.

**New gap noticed during the real-browser pass (not part of this fix, not yet actioned):** "Raw Seamoss — 200g," "Seamoss Powder — 250g," and "Seamoss Powder — 500g" render an empty placeholder box instead of a product photo on the Shop grid — those 3 dummy products appear to have no featured image assigned (a content/data gap, not a template bug; the other 5 dummy products display correctly).

| Page | Source mockup | Status | Notes |
|---|---|---|---|
| Shop / Collection archive (category-aware, paginated) | `collection.html` | Review | `woocommerce/archive-product.php` + `content-product.php` overrides — editorial header, category filter/tab row (real, count-agnostic), data-driven badge (sale % / Best Seller), quick-add overlay (real AJAX add-to-cart, relabeled "Quick Add"), native sort dropdown restyled. "FORM" filter from the mockup dropped as redundant with the category tabs (build decision) |
| Product Detail Page (shared template for any product, any category) | `product.html` | Review | `content-single-product.php` override — click-to-swap gallery (Alpine; dummy products' gallery images are the same photo repeated, a photography gap not a mechanism gap), sticky purchase panel with one-time/subscription radio (Alpine price preview; subscription checkout itself is honestly labeled as a Phase 9 dependency — selecting it today still adds the item at one-time price), real Add to Bag, placeholder WhatsApp number (needs the client's real number before launch), Customer Reviews section directly after the hero, Benefits/Purely Sourced/Daily Ritual/Testimonials sections reusing `nia/benefit-grid` and `nia/testimonial` blocks, Related Products restyled via a new `woocommerce/single-product/related.php` override (WC's default was an unstyled bare section/heading/grid that didn't match the rest of the page) |
| Product Reviews & Ratings (PDP, directly after the hero/purchase panel — moved there per client request, 2026-07-08) | none — added post-mockup, per client request | Completed (2026-07-08) | Not a new subsystem — re-surfaces WooCommerce's native review system (comments with `comment_type=review` + `rating` commentmeta; average/count cached on the product; admin moderates via wp-admin → Comments like any other comment) inside the new design, since it was dropped when Phase 6 removed the default PDP tabs. New: average-rating summary + clickable star-breakdown filter (`?rating_filter=`), sort control (`?review_sort=`: Recent/Highest/Lowest/Most Helpful), review cards (avatar, Verified Purchase badge, stars, content), and a "Helpful" vote button — genuinely new state (WP/WC have no concept of it), stored in commentmeta with one-vote-per-person dedup (user meta if logged in, a cookie for guests), toggled via a small AJAX endpoint (`Nia_Reviews` in nia-core). "Write a review" reuses WordPress's real `comment_form()`/submission pipeline (verified-owner gating, moderation) with a CSS-only star-radio picker, not a custom pipeline |
| Product category setup (4 initial: Seamoss Powder, Seamoss Gel, Raw Seamoss, Seamoss Capsules) | none — WooCommerce taxonomy setup | Completed (2026-07-08) | 4 categories created via Products → Categories, each with a thumbnail + description. 8 dummy products seeded (2 per category), 4 marked Featured, cross-sell relationships set. Admin-manageable — see note below. **Known gap (found 2026-07-08 during real-browser QA):** 3 of the 8 dummy products ("Raw Seamoss — 200g," "Seamoss Powder — 250g," "Seamoss Powder — 500g") have no featured image set and show an empty placeholder box on the Shop grid — needs a featured image assigned to each (Products → [product] → Product image), not a template/code fix |
| Cart | `my-cart.html` | Review | Re-themed via CSS (WooCommerce Blocks' stable class names) + block-content edits: cross-sells section relabeled "Complete Your Ritual" (WooCommerce's native, real `_crosssell_ids`-driven cross-sells block, restyled rather than rebuilt), trust row + same-day-delivery microcopy added as sibling blocks in the page content |
| Checkout | `checkout.html` | Review | Re-themed to the canonical palette (source mockup used the stale olive-gold set). Native `showFormStepNumbers` checkout-block option enables numbered steps (no custom JS). Trust badges + a payment-method note ("Mobile Money/Card launch with Selcom — Cash on Delivery is enabled for testing today") added via block-content edits. **VAT 18% tax rate configured** for Tanzania (`woocommerce_calc_taxes` enabled, TZ rate added) — verified via a real test order (tax + total calculated correctly, order deleted after). **Also fixed:** WooCommerce's store base country was still the default `US:CA`; corrected to `TZ` so tax/shipping calculate against the right base location |
| My Account / Customer Dashboard | `dashboard.html` | Review | `myaccount/my-account.php` + `navigation.php` overrides — dark-gradient sidebar (desktop, fixed) + the same hamburger/drawer mobile pattern as the main nav (its own Alpine scope). New page template `page-myaccount.php` assigned to the My Account page (page.php's narrow container didn't fit this layout) |
| My Account → Overview | `dashboard.html` | Review | `myaccount/dashboard.php` override — welcome header, real order-based status indicator, My Rituals summary card, real recent-order preview, Wellness Journal aside pulling the 2 latest real Journal posts |
| My Account → My Rituals (subscriptions) | `dashboard.html` (subscriptions panel) | Review | Real custom endpoint (`my-rituals`), honest empty state — no fabricated subscription data; explicitly depends on Phase 9 |
| My Account → Wellness Profile | `dashboard.html` (sidebar nav item) | Review | Real custom endpoint (`wellness-profile`) — no wellness-profile data model was ever scoped in `ARCHITECTURE.md`, so this is a genuine landing spot (links to the Journal) rather than fabricated personalization |
| My Account → Order History | `dashboard.html` (order history panel) | Review | Native "Orders" endpoint, restyled to the "app card" table treatment (`DESIGN_SYSTEM.md` §5) via CSS only, verified against a real test order |

**Where the admin manages the catalog day-to-day (no developer involvement needed):**
- **Products → Categories:** add/rename/delete/reorder categories, edit each category's thumbnail and description (the `taxonomy-terms-order` plugin, already active, adds drag-and-drop reordering here).
- **Products → All Products / Add New:** add/edit/delete products, set price (TZS — store base currency), stock, images, and mark/unmark the "Featured" checkbox in the Publish box (drives the homepage's Featured Products section automatically — see `ARCHITECTURE.md` §9a).
- **WooCommerce → Orders:** view/manage all customer orders (HPOS).
- **WooCommerce → Settings → Payments:** currently only "Cash on Delivery" is enabled as a placeholder — swapped for the real Selcom gateway in Phase 7.
- **WooCommerce → Settings → Shipping:** one placeholder flat-rate method ("Standard Delivery," 5,000 TZS, applies everywhere) — the client should confirm real shipping zones/rates before launch.
- All of the above requires zero code changes; this is the same "admin manages content, developer manages code" split already established for the Journal (blog posts) in Phase 4.

## Global Components (Phase 3 — prerequisite for all pages above)

**Phase 3 — done 2026-07-07.** Built in `wp-content/themes/nia-theme` (header/footer/CSS/forms) and the new `wp-content/plugins/nia-core` (Gutenberg blocks). Verified via a dev-only style-guide page (`template-style-guide.php`, page `/nia-style-guide/`, noindexed) showing every button/card/block variant, plus header/footer tested live on the homepage, shop archive, and a 404. All 8 theme files + 23 plugin files pass WPCS clean.

| Component | Status | Notes |
|---|---|---|
| Header / desktop nav | Completed (2026-07-07) | `header.php` — glassmorphic fixed nav, `wp_nav_menu` with `Nia_Nav_Walker` for active-state (falls back to static links until a 'primary' menu is assigned in Phase 4) |
| Mobile nav (hamburger/drawer) | Completed (2026-07-07) | Alpine.js-driven full-screen overlay drawer in `header.php`, per client-approved pattern — see `DESIGN_SYSTEM.md` §7 |
| Currency switcher (TZS/USD, header control) | Completed — UI only (2026-07-07) | Static `TZS ▾` control in header; real conversion logic via WPML Multicurrency lands in Phase 5, see `ARCHITECTURE.md` §11 |
| Language switcher (EN/SW, header control) | Completed — UI only (2026-07-07) | Static `EN`/`SW` text-label control in header; wired to WPML in Phase 5, see `DESIGN_SYSTEM.md` §10 |
| Elementor Saved Template library (on-brand components) | Completed (2026-07-07) | 6 templates (Hero, Benefit Grid, Testimonial, CTA Banner, Newsletter, Promo Tile) in Elementor's Template Library, built via `_elementor_data` + Global Colors/Fonts (Phase 2) + compiled Tailwind classes on widgets — see `ARCHITECTURE.md` §2a for the `css_classes`/`_css_classes` key distinction discovered while building these |
| Footer (full) | Completed (2026-07-07) | `footer.php` — 4-col grid (brand, Discover, Support, Newsletter), verbatim copyright line |
| Footer (minimal, checkout variant) | Completed (2026-07-07) | `footer-minimal.php`, auto-selected via `is_checkout()` (same mechanism as the header) |
| Button variants (8 total) | Completed (2026-07-07) | `.btn-primary`, `.btn-outline-light`, `.btn-outline-dark`, `.btn-inverse-surface`, `.btn-primary-filled`, `.btn-link`, `.btn-icon-circle`, `.btn-radio-card` in `src/css/input.css` — see `DESIGN_SYSTEM.md` §4 |
| Product card | Completed (2026-07-08) | `.card-product`/`.card-product-image` (Phase 3) + the full WooCommerce template wiring in `content-product.php` (Phase 6) — badge, quick-add overlay, count-agnostic for any category |
| Category filter/tab row (Shop archive) | Completed (2026-07-08) | Built into `archive-product.php`, reuses the journal/FAQ tab pattern per `DESIGN_SYSTEM.md` §14 item 12, scales to any number of categories |
| `.sunlight-shadow` card utility | Completed (Phase 2) | Already existed as of Phase 2; card variants built on top of it this phase (`.card-journal`, `.card-testimonial`, `.card-subscription-tier`, `.card-app`) |
| Testimonial block | Completed (2026-07-07) | Custom Gutenberg block `nia/testimonial` (nia-core plugin) — star rating, italic pull-quote, avatar/name/role, repeater |
| Benefit/Icon Grid block ("92 Minerals" pattern) | Completed (2026-07-07) | Custom Gutenberg block `nia/benefit-grid` — icon + title + body repeater, flexible item count |
| CTA Banner block | Completed (2026-07-07) | Custom Gutenberg block `nia/cta-banner` — 3 background variants, up to 2 CTAs |
| Newsletter Signup block | Completed — UI only (2026-07-07) | Custom Gutenberg block `nia/newsletter-signup` — email capture UI only, no backend wired yet |
| Dashboard sidebar (desktop) | Completed (2026-07-08) | `myaccount/navigation.php` override — fixed dark-gradient sidebar, real My Account endpoints |
| Dashboard sidebar (mobile equivalent) | Completed (2026-07-08) | Same file — its own Alpine hamburger/drawer scope, same visual pattern as the main nav per `DESIGN_SYSTEM.md` §7 |

## Excluded from build

| File | Reason |
|---|---|
| `about.html` | Abandoned "Aureate Wellness" rebrand concept — different brand name, fonts, colors. Not linked from any live page's nav. Superseded by `about-nia.html`. |
| `ritual.html` **or** `subscription.html` (whichever is not chosen) | Near-duplicate pages — only one should be built as a WordPress template. |

---

## Legend for future entries

When adding a new page not listed above, append it to the correct section with: source mockup (or "none — new content"), status, and any notes on design decisions/gaps that had to be made during build (cross-reference `DESIGN_SYSTEM.md`/`ARCHITECTURE.md` if a decision was logged there).
